model NormasTags {
  norma    Norma  @relation(fields: [id_norma], references: [id], onDelete: Cascade)
  id_norma String
  tag      Tag    @relation(fields: [id_tag], references: [id], onDelete: Cascade)
  id_tag   String

  @@id([id_tag, id_norma])
}
